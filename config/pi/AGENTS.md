# Global agent context

## Conventions

- Prefer `rg` and `fd` over recursive `grep` and `find` when available.
- Do not commit or push unless explicitly asked.
- Keep changes scoped to what was requested.
- Say so before starting a long-running command.
- Use the configured editor from `$EDITOR` when opening files for the user.
