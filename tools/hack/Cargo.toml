[package]
name = "hack"
version = "0.1.0"
edition = "2021"
description = "A small, catalog-driven Git worktree manager"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
