;;; post.el --- example late private layer -*- lexical-binding: t; -*-

;; Define private commands and keybindings here, after the public configuration
;; has loaded.

;; A layer may also select its remote/subscription AI provider:
;; (after! gptel
;;   (my/gptel-register-primary-backend YOUR-BACKEND 'YOUR-MODEL))
